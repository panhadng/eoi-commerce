/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["product-matching-168.s3.us-west-2.amazonaws.com"],
  },
};

export default nextConfig;
